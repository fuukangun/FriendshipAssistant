# FriendshipAssistant 领域词汇

## 送礼推荐弹窗

玩家与 NPC 对话结束后出现的送礼选择界面。它展示当前可用的礼物，并允许玩家选择、送出或暂时关闭提示。

## 手柄焦点

当前由手柄方向输入选中的可交互元素。焦点必须始终对应一个可执行的菜单操作或可送出的礼物。

## 可视区域底行

当前屏幕所显示礼物网格中的最后一行。它不一定是当前页面的最后一行；如果后面还有礼物，继续向下导航应先滚动列表。

## 页面最后一行

当前背包页或存储页中所有礼物的最后一行。焦点在这一行时继续向下，才会进入底部操作区。

## 今日不再提示

针对指定 NPC、游戏年份、季节和日期，暂时不再显示送礼推荐弹窗的操作。该操作不等于关闭菜单，也不等于送出礼物。

## 主动焦点

由最近一次输入设备控制的菜单焦点。鼠标和手柄可以交替使用，最近发生交互的输入设备拥有焦点控制权。

## 列夹取

焦点滚动到新区域后，如果原列在目标行不存在，则将焦点移动到该行最右侧的最后一个可用礼物。列夹取不允许焦点落在空白格或分类标题上。

## 手柄 tooltip

由手柄焦点触发的礼物名称和原版描述提示。它锚定在当前礼物格附近，并根据菜单和屏幕边界选择显示方向；鼠标 tooltip 则继续跟随鼠标位置。
